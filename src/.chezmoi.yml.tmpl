name: "Josh Sherman"
{{ $email := promptString "email" -}}
gpgKeyId: "34230383"

merge:
  command: "nvim"
  args:
    - "d"
    - "{{ "{{" }} .Destination {{ "}}" }}"
    - "{{ "{{" }} .Source {{ "}}" }}"
    - "{{ "{{" }} .Target {{ "}}" }}"

encryption: "gpg"
gpg:
  recipient: "{{ $email }}"

hooks:
  read-source-state:
    pre:
      command: "~/.local/share/chezmoi/src/.chezmoiscripts/.install-password-manager.sh"

git:
  autoCommit: true

progress: true

data:
  email: {{ $email | quote }}
  editor: nvim
.gnupg
.gnupg/**
