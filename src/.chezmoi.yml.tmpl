name: "Josh Sherman"
{{ $email := promptString "email" -}}
gpgKeyId: "34230383"

merge:
  command: "nvim"
  args:
    - "d"
    - "{{ "{{" }} .Destination {{ "}}" }}"
    - "{{ "{{" }} .Source {{ "}}" }}"
    - "{{ "{{" }} .Target {{ "}}" }}"

encryption: "gpg"
gpg:
  recipient: "34230383"

hooks:
  read-source-state:
    pre:
      command: "/Users/jsherman/.local/share/chezmoi/src/.chezmoiscripts/.install-password-manager.sh"

git:
  autoCommit: true

progress: true

data:
  email: {{ $email | quote }}
  editor: nvim
